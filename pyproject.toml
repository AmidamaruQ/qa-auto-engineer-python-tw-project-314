[project]
name = "hexlet-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.3",
    "selenium>=4.43.0",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
    "ruff>=0.15.11",
]
